{
  "version": 2,
  "builds": [
    {
      "src": "learning_language/pubspec.yaml",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "learning_language/build/web",
        "buildCommand": "bash build.sh"
      }
    }
  ]
}
